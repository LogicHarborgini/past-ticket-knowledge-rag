[pytest]
# rootdir on sys.path so `import app` works when pytest is invoked directly.
# asyncio_mode=auto would also work, but explicit @pytest.mark.asyncio markers
# make it visible in the test file which tests drive the async pipeline.
testpaths = tests
asyncio_mode = strict
asyncio_default_fixture_loop_scope = function
